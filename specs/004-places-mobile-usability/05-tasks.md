# Places mobile usability - Tasks

- `TASK-01` (`REQ-001`, `REQ-002`): add browser regressions for mobile bounds
  and the explicit return link; implement the compact layout and link.
- `TASK-02` (`REQ-003`, `REQ-004`): change the existing action test to require a
  public owner-scoped read; remove only the read-session gate.
- `TASK-03` (`REQ-005`): change existing scoring expectations to 10 km, observe
  RED, then update the city radius constant and integration expectation.
- `TASK-04`: update Places UI/design/operational docs and the evidence matrix.
- `TASK-05`: run focused tests, mobile E2E, lint, typecheck, full test, build,
  diff check, and convergence review.
